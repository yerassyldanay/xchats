# Every entry here must be a collaborator with at least write access — GitHub
# silently ignores an owner entry for anyone without it, which would make
# that line a no-op rather than an error.
* @yerassyldanay @yessaliyev
