feedback

# secrets / local config (commit only the *.example.* files)
.env
config.yaml
backend/.env
backend/config.yaml
frontend/.env

# local-only deploy helpers (machine-specific host ports; restore script holds a
# local Evolution API key) — keep out of git
deploy/docker-compose.override.yaml
deploy/restore-evolution-webhook.sh

# blob store (local-disk media bytes)
blobdata/
backend/blobdata/

# build output
backend/bin/
backend/xchats
frontend/dist/
frontend/dist-ssr/
frontend/node_modules/
node_modules/

# playwright e2e artifacts
frontend/test-results/
frontend/playwright-report/
frontend/blob-report/
frontend/playwright/.cache/

# go
*.test

# editor / os
.DS_Store
*.swp

.claude