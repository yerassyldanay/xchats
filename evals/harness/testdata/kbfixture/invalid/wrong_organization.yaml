# Fail-closed rule under test: the referenced material belongs to a
# DIFFERENT organization than the fixture's own organization_id.
# kbd_materials[].organization_id is normally left unset (it defaults to the
# fixture's organization_id — see types.go's KBDMaterial doc comment); this
# is the one legitimate reason to set it explicitly.
organization_id: "33333333-3333-3333-3333-333333333333"
ai_products:
  - ref: probe
    name: "Тестовый товар"
    price: "1 000 ₸"
    in_stock: true
    sales_status: active
    featured_image: "44444444-4444-4444-4444-444444444444"
kbd_materials:
  - id: "44444444-4444-4444-4444-444444444444"
    organization_id: "99999999-9999-9999-9999-999999999999"
    source_type: file
    filename: "probe.jpg"
    mime_type: "image/jpeg"
    size_bytes: 1000
    storage_backend: fake
    storage_key: "fake://probe"
    processing_status: built
    customer_visibility: visible
