# GENERATED by harness render — do not hand-edit. Edit scenario.yaml / data.yaml / tests.yaml instead.
description: |
    Phase 2.1 language architecture bake-off, variant V4 (language-routed frame) — the RU branch. Together with lang-canary-v4-kk, represents what a production router (detectLang) would send: a message classified "ru" gets this UNCHANGED V1 frame, a message classified "kk" gets the full Kazakh frame in the sibling scenario. Splitting into two scenarios avoids needing runtime prompt routing inside the harness/promptfoo — detectLang's own unit tests already prove these are the messages it would route here.
prompts:
    - id: file://prompt.txt
      label: lang-canary-v4-ru
providers:
    - id: openrouter:google/gemini-2.5-flash-lite
      config:
        max_tokens: 500
        temperature: 0.3
    - id: openrouter:google/gemini-2.5-flash
      config:
        max_tokens: 500
        temperature: 0.3
    - id: openrouter:deepseek/deepseek-v3.2-exp
      config:
        max_tokens: 500
        temperature: 0.3
tests:
    - description: 1. price question, Russian
      vars:
        history: (пусто — начало разговора)
        message: Сколько стоит кофемашина?
      assert:
        - type: javascript
          value: "true"
    - description: 9. off-KB city coverage
      vars:
        history: (пусто — начало разговора)
        message: А вы доставляете в Астану?
      assert:
        - type: javascript
          value: "true"
    - description: 20. mixed Kazakh/Russian message, rule says answer Russian when mixed
      vars:
        history: (пусто — начало разговора)
        message: Сәлеметсіз бе! Скажите, пожалуйста, кофемашина DeLonghi қанша тұрады?
      assert:
        - type: javascript
          value: "true"
    - description: 16. follow-up with history, needs delivery cost
      vars:
        history: |-
            Клиент: Сколько стоит кофемашина DeLonghi?
            Ассистент: Кофемашина DeLonghi стоит 129 900 ₸.
        message: А доставка сколько будет стоить?
      assert:
        - type: javascript
          value: "true"
