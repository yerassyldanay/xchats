# GENERATED by harness render — do not hand-edit. Edit scenario.yaml / data.yaml / tests.yaml instead.
description: |
    Phase 2.3 combined-variant bake-off, V1 — the KK branch: lang-canary-v4-kk's full Kazakh frame with escalation-canary-v2's strengthened rule 1 translated into it, plus the NEW "the KNOWLEDGE BASE may be INCOMPLETE — never claim we do/don't have something that isn't in it; escalate instead" rule. Sent only when detectLang (langdetect.go) classifies the customer's message as Kazakh — including genuinely-mixed messages whose QUESTION clause is Kazakh (test 20), per the dominant-language rule. This is the branch that finally puts both bake-off winners in front of test 21 (Kazakh escalation), which failed in EVERY variant of run 2026-07-19_02-50-37-ef6e because no frame carried both fixes. KB/FACTS injected data stays Russian (no knowledge-base duplication, per the plan's explicit constraint). DRAFT translation — NOT yet reviewed by a native Kazakh speaker; the plan requires that review before this variant is used in any billed comparison treated as final (a mistranslated instruction would invalidate the whole experiment).
prompts:
    - id: file://prompt.txt
      label: combo-canary-v1-kk
providers:
    - id: openrouter:deepseek/deepseek-v3.2-exp
      config:
        max_tokens: 500
        temperature: 0.3
    - id: openrouter:google/gemini-2.5-flash
      config:
        max_tokens: 500
        temperature: 0.3
    - id: openrouter:google/gemini-2.5-flash-lite
      config:
        max_tokens: 500
        temperature: 0.3
tests:
    - description: 2. price question, Kazakh
      vars:
        history: (пусто — начало разговора)
        message: Кофемашина DeLonghi қанша тұрады?
      assert:
        - type: javascript
          value: "true"
    - description: 3. delivery cost + time, Kazakh
      vars:
        history: (пусто — начало разговора)
        message: Жеткізу қанша тұрады және қанша күнде жетеді?
      assert:
        - type: javascript
          value: "true"
    - description: 20. mixed Kazakh/Russian message — reply in the dominant language (the question itself is Kazakh)
      vars:
        history: (пусто — начало разговора)
        message: Сәлеметсіз бе! Скажите, пожалуйста, кофемашина DeLonghi қанша тұрады?
      assert:
        - type: javascript
          value: "true"
    - description: 21. off-KB city coverage, Kazakh — escalation must stay Kazakh with no invented claim
      vars:
        history: (пусто — начало разговора)
        message: Сіздер Астанаға жеткізесіздер ме?
      assert:
        - type: javascript
          value: "true"
