# GENERATED by harness render — do not hand-edit. Edit scenario.yaml / data.yaml / tests.yaml instead.
description: |
    Phase 2.3 combined-variant bake-off, V1 — the RU branch: merges the two prior winners (lang-canary-v4's routed frames, Phase 2.1; escalation-canary-v2's strengthened rule 1, Phase 2.2) and adds a NEW "the KNOWLEDGE BASE may be INCOMPLETE — never claim we do/don't have something that isn't in it; escalate instead" rule. Targets the deny-instead-of-escalate pattern (tests 18/19 here; run 2026-07-19_02-50-37-ef6e saw models answer "iPhone нет"/"не ремонтируем" — claims the KB never makes) and the language x escalation interaction (test 21, KK branch), which no single earlier variant could pass because no frame carried both fixes at once. Reuses shop-current's real knowledge base, same as every canary variant.
prompts:
    - id: file://prompt.txt
      label: combo-canary-v1-ru
providers:
    - id: openrouter:deepseek/deepseek-v3.2-exp
      config:
        max_tokens: 500
        temperature: 0.3
    - id: openrouter:google/gemini-2.5-flash
      config:
        max_tokens: 500
        temperature: 0.3
    - id: openrouter:google/gemini-2.5-flash-lite
      config:
        max_tokens: 500
        temperature: 0.3
tests:
    - description: 1. price question, Russian
      vars:
        history: (пусто — начало разговора)
        message: Сколько стоит кофемашина?
      assert:
        - type: javascript
          value: "true"
    - description: 9. off-KB city coverage
      vars:
        history: (пусто — начало разговора)
        message: А вы доставляете в Астану?
      assert:
        - type: javascript
          value: "true"
    - description: 10. refund request
      vars:
        history: (пусто — начало разговора)
        message: Верните мне, пожалуйста, деньги за прошлый заказ
      assert:
        - type: javascript
          value: "true"
    - description: 16. follow-up with history, needs delivery cost
      vars:
        history: |-
            Клиент: Сколько стоит кофемашина DeLonghi?
            Ассистент: Кофемашина DeLonghi стоит 129 900 ₸.
        message: А доставка сколько будет стоить?
      assert:
        - type: javascript
          value: "true"
    - description: 18. wrong understanding, asks about a product we don't sell
      vars:
        history: (пусто — начало разговора)
        message: У вас есть iPhone 15 Pro?
      assert:
        - type: javascript
          value: "true"
    - description: 19. wrong understanding, asks about a service we don't offer
      vars:
        history: (пусто — начало разговора)
        message: Вы кофемашины ремонтируете?
      assert:
        - type: javascript
          value: "true"
