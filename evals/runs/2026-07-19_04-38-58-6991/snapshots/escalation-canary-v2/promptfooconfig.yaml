# GENERATED by harness render — do not hand-edit. Edit scenario.yaml / data.yaml / tests.yaml instead.
description: |
    Phase 2.2 escalation-rule bake-off, variant V2: rule 1 (grounding/escalation) strengthened with an explicit ban on asserting ANYTHING about the business — positive or negative — when escalating, not just the specific observed phrasings the must_not_contain blocklists happen to catch. Language rule untouched (V1's, unchanged) — same canary set as escalation-canary-v1 so the two are directly comparable.
prompts:
    - id: file://prompt.txt
      label: escalation-canary-v2
providers:
    - id: openrouter:deepseek/deepseek-v3.2-exp
      config:
        max_tokens: 500
        temperature: 0.3
    - id: openrouter:google/gemini-2.5-flash
      config:
        max_tokens: 500
        temperature: 0.3
    - id: openrouter:google/gemini-2.5-flash-lite
      config:
        max_tokens: 500
        temperature: 0.3
tests:
    - description: 9. off-KB city coverage
      vars:
        history: (пусто — начало разговора)
        message: А вы доставляете в Астану?
      assert:
        - type: javascript
          value: "true"
    - description: 10. refund request
      vars:
        history: (пусто — начало разговора)
        message: Верните мне, пожалуйста, деньги за прошлый заказ
      assert:
        - type: javascript
          value: "true"
    - description: 18. wrong understanding, asks about a product we don't sell
      vars:
        history: (пусто — начало разговора)
        message: У вас есть iPhone 15 Pro?
      assert:
        - type: javascript
          value: "true"
    - description: 19. wrong understanding, asks about a service we don't offer
      vars:
        history: (пусто — начало разговора)
        message: Вы кофемашины ремонтируете?
      assert:
        - type: javascript
          value: "true"
    - description: 21. off-KB city coverage, Kazakh — escalation must stay Kazakh with no invented claim
      vars:
        history: (пусто — начало разговора)
        message: Сіздер Астанаға жеткізесіздер ме?
      assert:
        - type: javascript
          value: "true"
    - description: 16. follow-up with history, needs delivery cost
      vars:
        history: |-
            Клиент: Сколько стоит кофемашина DeLonghi?
            Ассистент: Кофемашина DeLonghi стоит 129 900 ₸.
        message: А доставка сколько будет стоить?
      assert:
        - type: javascript
          value: "true"
