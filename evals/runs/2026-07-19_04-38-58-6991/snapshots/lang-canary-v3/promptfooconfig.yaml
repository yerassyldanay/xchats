# GENERATED by harness render — do not hand-edit. Edit scenario.yaml / data.yaml / tests.yaml instead.
description: |
    Phase 2.1 language architecture bake-off, variant V3: the grounding, fact-token, and language rules are each stated in BOTH Russian and Kazakh inside one frame (no knowledge-base duplication — KB/FACTS stay Russian-only, per the plan's explicit constraint against doubling KB tokens). Tests whether Kazakh instruction PRESENCE alone breaks the Russian-context pull, without any routing machinery. DRAFT Kazakh wording — needs native-speaker review before any billed run (see Phase 2.1 note).
prompts:
    - id: file://prompt.txt
      label: lang-canary-v3
providers:
    - id: openrouter:deepseek/deepseek-v3.2-exp
      config:
        max_tokens: 500
        temperature: 0.3
    - id: openrouter:google/gemini-2.5-flash
      config:
        max_tokens: 500
        temperature: 0.3
    - id: openrouter:google/gemini-2.5-flash-lite
      config:
        max_tokens: 500
        temperature: 0.3
tests:
    - description: 1. price question, Russian
      vars:
        history: (пусто — начало разговора)
        message: Сколько стоит кофемашина?
      assert:
        - type: javascript
          value: "true"
    - description: 2. price question, Kazakh
      vars:
        history: (пусто — начало разговора)
        message: Кофемашина DeLonghi қанша тұрады?
      assert:
        - type: javascript
          value: "true"
    - description: 3. delivery cost + time, Kazakh
      vars:
        history: (пусто — начало разговора)
        message: Жеткізу қанша тұрады және қанша күнде жетеді?
      assert:
        - type: javascript
          value: "true"
    - description: 9. off-KB city coverage
      vars:
        history: (пусто — начало разговора)
        message: А вы доставляете в Астану?
      assert:
        - type: javascript
          value: "true"
    - description: 20. mixed Kazakh/Russian message — reply in the dominant language (the question itself is Kazakh)
      vars:
        history: (пусто — начало разговора)
        message: Сәлеметсіз бе! Скажите, пожалуйста, кофемашина DeLonghi қанша тұрады?
      assert:
        - type: javascript
          value: "true"
    - description: 21. off-KB city coverage, Kazakh — escalation must stay Kazakh with no invented claim
      vars:
        history: (пусто — начало разговора)
        message: Сіздер Астанаға жеткізесіздер ме?
      assert:
        - type: javascript
          value: "true"
    - description: 16. follow-up with history, needs delivery cost
      vars:
        history: |-
            Клиент: Сколько стоит кофемашина DeLonghi?
            Ассистент: Кофемашина DeLonghi стоит 129 900 ₸.
        message: А доставка сколько будет стоить?
      assert:
        - type: javascript
          value: "true"
