# GENERATED by harness render — do not hand-edit. Edit scenario.yaml / data.yaml / tests.yaml instead.
description: |
    Phase 2.1 language architecture bake-off, variant V4 (language-routed frame) — the KK branch: a full Kazakh translation of the frame, sent only when detectLang (langdetect.go) classifies the customer's message as Kazakh. KB/FACTS injected data stays Russian (no knowledge-base duplication, per the plan's explicit constraint) — this frame tests whether the MODEL can read a Russian KB and answer in Kazakh when its entire active instruction set is unambiguously Kazakh, with zero competing Russian instructions. DRAFT translation — NOT yet reviewed by a native Kazakh speaker; the plan requires that review before this variant is used in any billed comparison (a mistranslated instruction would invalidate the whole experiment).
prompts:
    - id: file://prompt.txt
      label: lang-canary-v4-kk
providers:
    - id: openrouter:deepseek/deepseek-v3.2-exp
      config:
        max_tokens: 500
        temperature: 0.3
    - id: openrouter:google/gemini-2.5-flash
      config:
        max_tokens: 500
        temperature: 0.3
    - id: openrouter:google/gemini-2.5-flash-lite
      config:
        max_tokens: 500
        temperature: 0.3
tests:
    - description: 2. price question, Kazakh
      vars:
        history: (пусто — начало разговора)
        message: Кофемашина DeLonghi қанша тұрады?
      assert:
        - type: javascript
          value: "true"
    - description: 3. delivery cost + time, Kazakh
      vars:
        history: (пусто — начало разговора)
        message: Жеткізу қанша тұрады және қанша күнде жетеді?
      assert:
        - type: javascript
          value: "true"
    - description: 20. mixed Kazakh/Russian message — reply in the dominant language (the question itself is Kazakh)
      vars:
        history: (пусто — начало разговора)
        message: Сәлеметсіз бе! Скажите, пожалуйста, кофемашина DeLonghi қанша тұрады?
      assert:
        - type: javascript
          value: "true"
    - description: 21. off-KB city coverage, Kazakh — escalation must stay Kazakh with no invented claim
      vars:
        history: (пусто — начало разговора)
        message: Сіздер Астанаға жеткізесіздер ме?
      assert:
        - type: javascript
          value: "true"
