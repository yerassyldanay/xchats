name: escalation-canary-v1
description: >
  Phase 2.2 escalation-rule bake-off, variant V1 (control): shop-current's frame,
  byte-for-byte unchanged, against a focused escalation-trap canary set. Held as a
  separate axis from the Phase 2.1 language bake-off on purpose (experiment doctrine:
  "keep prompt quality separate ... attributable") — this tests ONLY the escalation
  wording, language rule untouched.
setup: escalation-v1
prompt_ref: escalation-control@v1
experiment: escalation-bakeoff
archived: true
archived_reason: "Superseded by the schema_kb_v1 consolidation (2026-07-23) — strengthened escalation rules now live in the shop-kb-v1 frame; this scenario's historical runs are preserved unchanged."
frame: ../lang-canary-v1/frame.txt
data: ../shop-current/data.yaml
tests: tests.yaml
topic_format: "# topic: {ref} ({lang})\nkeywords: {keywords}\n{body}"
