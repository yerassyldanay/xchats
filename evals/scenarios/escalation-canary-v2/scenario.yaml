name: escalation-canary-v2
description: >
  Phase 2.2 escalation-rule bake-off, variant V2: rule 1 (grounding/escalation)
  strengthened with an explicit ban on asserting ANYTHING about the business — positive
  or negative — when escalating, not just the specific observed phrasings the
  must_not_contain blocklists happen to catch. Language rule untouched (V1's, unchanged)
  — same canary set as escalation-canary-v1 so the two are directly comparable.
setup: escalation-v2
prompt_ref: escalation-strengthened@v2
experiment: escalation-bakeoff
archived: true
archived_reason: "Superseded by the schema_kb_v1 consolidation (2026-07-23) — strengthened escalation rules now live in the shop-kb-v1 frame; this scenario's historical runs are preserved unchanged."
frame: frame.txt
data: ../shop-current/data.yaml
tests: ../escalation-canary-v1/tests.yaml
topic_format: "# topic: {ref} ({lang})\nkeywords: {keywords}\n{body}"
