name: lang-canary-v2
description: >
  Phase 2.1 language architecture bake-off, variant V2: shop-current's frame with the
  language rule (only) strengthened to a more explicit single sentence — the cheap
  comparator. If V2 alone reaches the Phase 3 gate, the architecture question (V3/V4) is
  moot; if it doesn't, that failure is itself evidence for the plan's hypothesis. Shares
  V1's canary test set and knowledge base — only the frame's language rule differs.
frame: frame.txt
data: ../shop-current/data.yaml
tests: ../lang-canary-v1/tests.yaml
contract: asset_refs
topic_format: "# topic: {ref} ({lang})\nkeywords: {keywords}\n{body}"
