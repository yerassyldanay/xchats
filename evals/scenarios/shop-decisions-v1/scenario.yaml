name: shop-decisions-v1
description: "Shop schema per DECISIONS.md: language-neutral fact values, grouped media columns"
frame: frame.txt
data: data.yaml
tests: tests.yaml
topic_format: "# {ref} — {title} ({keywords})\n{body}"
