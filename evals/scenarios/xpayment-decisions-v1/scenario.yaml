name: xpayment-decisions-v1
description: "Payments-SaaS schema per DECISIONS.md: tariffs instead of products, grouped media columns"
frame: frame.txt
data: data.yaml
tests: tests.yaml
topic_format: "# {ref} — {title} ({keywords})\n{body}"
